# VITE_API_URL should be set via build arguments in Docker
# For Railway: Set this as a build-time environment variable in Railway dashboard
# Default fallback for local production builds
VITE_API_URL=http://localhost:8000
